import {readFileSync,writeFileSync} from 'node:fs';\nconst read=p=>readFileSync(new URL('../'+p,import.meta.url),'utf8');\nlet html=read('v50.html');\nconst patch=read('src/v51/mission-first.js');\nif(!html.includes('const clock=new THREE.Clock();'))throw Error('V50 clock insertion point missing');\nhtml=html\n .replace('Emberwing V50 — Closing Door','Emberwing V51 — Mission First')\n .replace('src/v50/flight.css','src/v51/flight.css')\n .replace('const clock=new THREE.Clock();',patch+'\nconst clock=new THREE.Clock();');\nwriteFileSync(new URL('../v51.html',import.meta.url),html);\nconsole.log('Built V51 from V50 plus mission-first guidance.');\n