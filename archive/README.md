# Emberwing Archive

Historical prototypes and release notes preserved for reference.

- `versions/`: numbered HTML experiments from earlier iterations
- `releases/`: historical release notes

These files are not inputs to the current Level 1 build. The canonical playable entry points remain `index.html` and `play.html`, backed by `src/level1/`.
