import {readFileSync,writeFileSync} from 'node:fs';\nconst read=p=>readFileSync(new URL('../'+p,import.meta.url),'utf8');\nlet html=read('v53.html');\nconst patch=read('src/v55/long-throat.js');\nif(!html.includes('const clock=new THREE.Clock();'))throw Error('V53 clock insertion point missing');\nhtml=html\n .replace('Emberwing V53 — Basin Reveal','Emberwing V55 — Long Throat')\n .replace('src/v53/flight.css','src/v55/flight.css')\n .replace('const clock=new THREE.Clock();',patch+'\nconst clock=new THREE.Clock();');\nwriteFileSync(new URL('../v55.html',import.meta.url),html);\nconsole.log('Built V55 from V53 plus authored Long Throat level.');\n